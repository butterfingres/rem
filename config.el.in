;; -*- lexical-binding: t; -*-

(defconst config-emacs "@EMACS@")
(defconst config-project-root "@SRCDIR@")
(defconst config-module-dir (expand-file-name "@CARGO_OUTPUT_DIR@" config-project-root))

(provide 'config)
