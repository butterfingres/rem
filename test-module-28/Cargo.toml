[package]
name = "test-module-28"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rem = { path = "..", features = ["emacs-28"] }

[features]
bindgen = ["rem/bindgen"]
